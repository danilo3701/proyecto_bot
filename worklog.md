# WORKLOG проекта

## Правило
- Каждая попытка фикса = новая запись
- В записи: симптом → что сделали → результат
- Если ❌ не сработало: пишем почему/что увидели в логах

---

## Записи

### 2026-02-13 · staging (grammar) · ✅
Симптом: TelegramConflictError на тестовом сервисе (copy) и на проде
Сделали:
- BOT_TOKEN теперь берётся из Railway Variables (env), убрали хардкод из core8_1.py
- В Railway: prod BOT_TOKEN = основной, copy BOT_TOKEN = тестовый
Результат: конфликт исчез, copy успешно деплоится, тестовый бот отвечает
Коммит: "Fix: BOT_TOKEN из переменных Railway"
